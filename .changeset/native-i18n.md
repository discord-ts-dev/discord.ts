---
'@discord.ts/common': minor
'@discord.ts/core': minor
---

Add native i18n: enable with `i18n` in discord.config.ts, catalogs in `src/locales/<lang>/<namespace>.json`, lookup via `t()`, per-call locale via `@Locale()`
