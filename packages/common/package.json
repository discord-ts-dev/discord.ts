{
  "name": "@discord.ts/common",
  "version": "0.1.0",
  "description": "discord.ts foundation: metadata keys, decorators, types, logger",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "oxlint .",
    "format": "oxfmt --write src",
    "test": "bun test"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "discord.js": "^14.0.0",
    "reflect-metadata": "^0.2.2",
    "signale": "^1.4.0"
  },
  "devDependencies": {
    "@types/signale": "^1.4.7",
    "typescript": "^5.6.3"
  },
  "peerDependencies": {
    "discord.js": "^14.0.0"
  }
}
