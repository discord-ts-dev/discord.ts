{
  "name": "discord.ts",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "turbo run build && bun run postinstall",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "format:check": "oxfmt --check apps packages",
    "postinstall": "node -e \"try{require('fs').chmodSync('packages/cli/dist/cli.js',0o755)}catch{};require('fs').mkdirSync('apps/example/node_modules/.bin',{recursive:true});try{require('fs').unlinkSync('apps/example/node_modules/.bin/discord')}catch{};require('fs').symlinkSync('../../../../packages/cli/dist/cli.js','apps/example/node_modules/.bin/discord')\""
  },
  "devDependencies": {
    "oxfmt": "^0.67.0",
    "oxlint": "^1.82.0",
    "turbo": "^2.5.4",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
